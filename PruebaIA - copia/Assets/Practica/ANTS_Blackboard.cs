using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ANTS_Blackboard : MonoBehaviour
{
    public GameObject cabbageLocation;
    public GameObject nestLocation;
    public float chickenFarAwayRadius = 10f;
    public float chickenTooClose = 5f;
    public float locationDetectionRadius = 5f;

    // Start is called before the first frame update
    
    // Update is called once per frame
    void Update()
    {
        
    }
}
